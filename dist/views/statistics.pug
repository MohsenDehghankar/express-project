extends layout

block content
    a(href='//localhost:3002') Main Page
    br
    br
    br
    h3 IP addresses ( #{ips.length} )
    ul
        each val in ips 
            li= val
    h3 Times ( #{times.length} )
    ul
        each val in times
            li= val